name: maps
version: 1.0.0
category: navigation
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - generate map
  - show on map
triggers:
  - "show map of"
  - "visualize location"
  - "map"
