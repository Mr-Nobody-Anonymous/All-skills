name: geocode
version: 1.0.0
category: navigation
priority: 15
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - geocode address
  - get coordinates
triggers:
  - "where is"
  - "coordinates of"
  - "address to location"
