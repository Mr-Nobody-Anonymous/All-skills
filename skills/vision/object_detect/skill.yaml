name: object_detect
version: 1.0.0
category: vision
priority: 10
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - detect objects
  - find objects in image
triggers:
  - "what objects are in"
  - "detect things in"
  - "find objects"
