name: vqa
version: 1.0.0
category: vision
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - visual question answering
  - ask about image
triggers:
  - "what color is"
  - "how many are there"
  - "is there a"
