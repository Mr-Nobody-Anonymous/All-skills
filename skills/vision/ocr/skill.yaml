name: ocr
version: 1.0.0
category: vision
priority: 20
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - extract text from image
  - ocr
  - read text in image
triggers:
  - "read the text in"
  - "what does this image say"
