name: document_ai
version: 1.0.0
category: vision
priority: 20
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - parse document
  - extract form data
triggers:
  - "extract data from document"
  - "parse this form"
