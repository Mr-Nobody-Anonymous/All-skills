name: docker
version: 1.0.0
category: devtools
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - docker build
  - docker run
  - container management
triggers:
  - "docker"
  - "container"
  - "build image"
