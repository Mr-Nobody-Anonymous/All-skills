name: code_lint
version: 1.0.0
category: devtools
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - lint code
  - check code quality
triggers:
  - "lint this"
  - "code quality"
  - "check for errors"
