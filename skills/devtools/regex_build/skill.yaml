name: regex_build
version: 1.0.0
category: devtools
priority: 30
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - build regex
  - test regex pattern
triggers:
  - "regex for"
  - "pattern to match"
  - "regex"
