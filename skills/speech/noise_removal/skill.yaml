name: noise_removal
version: 1.0.0
category: speech
priority: 10
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - remove noise
  - denoise audio
  - enhance speech
triggers:
  - "clean up this audio"
  - "remove background noise"
