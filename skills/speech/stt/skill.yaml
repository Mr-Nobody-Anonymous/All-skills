name: stt
version: 1.0.0
category: speech
priority: 5
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - transcribe speech
  - speech to text
triggers:
  - "transcribe this"
  - "what did I say"
  - "convert speech"
