name: color_palette
version: 1.0.0
category: creative
priority: 35
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - generate color palette
  - color scheme
triggers:
  - "color palette"
  - "color scheme"
  - "complementary colors"
