name: poetry
version: 1.0.0
category: creative
priority: 30
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - write poem
  - poetry generation
triggers:
  - "write a poem"
  - "haiku"
  - "poetry about"
