name: calendar
version: 1.0.0
category: communication
priority: 15
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - manage calendar
  - create event
  - check schedule
triggers:
  - "schedule meeting"
  - "add to calendar"
  - "what's on my calendar"
