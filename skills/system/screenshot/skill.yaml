name: screenshot
version: 1.0.0
category: system
priority: 20
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - take screenshot
  - capture screen
triggers:
  - "screenshot"
  - "capture screen"
  - "take photo of screen"
