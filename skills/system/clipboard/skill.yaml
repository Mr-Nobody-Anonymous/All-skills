name: clipboard
version: 1.0.0
category: system
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - copy to clipboard
  - read clipboard
triggers:
  - "copy this"
  - "paste from clipboard"
  - "what's in clipboard"
