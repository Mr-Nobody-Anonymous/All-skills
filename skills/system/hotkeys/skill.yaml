name: hotkeys
version: 1.0.0
category: system
priority: 15
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - register hotkey
  - keyboard shortcut
triggers:
  - "bind key"
  - "hotkey"
  - "keyboard shortcut"
