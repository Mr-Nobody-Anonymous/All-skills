name: ping
version: 1.0.0
category: networking
priority: 10
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - ping host
  - check connectivity
triggers:
  - "ping"
  - "is online"
  - "reachable"
