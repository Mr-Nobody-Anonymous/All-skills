name: wake_on_lan
version: 1.0.0
category: networking
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - wake on lan
  - wol
triggers:
  - "wake up computer"
  - "wake on lan"
  - "wol"
