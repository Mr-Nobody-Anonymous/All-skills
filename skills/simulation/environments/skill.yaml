name: environments
version: 1.0.0
category: simulation
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - rl environment
  - gym environment
triggers:
  - "gymnasium"
  - "rl env"
  - "reinforcement learning env"
