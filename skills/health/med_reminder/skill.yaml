name: med_reminder
version: 1.0.0
category: health
priority: 5
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - set medication reminder
  - medicine schedule
triggers:
  - "remind me to take"
  - "medication reminder"
  - "pill"
