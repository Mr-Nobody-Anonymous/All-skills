name: network_scan
version: 1.0.0
category: security
priority: 20
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - scan network
  - port scan
triggers:
  - "scan ports"
  - "network scan"
  - "open ports on"
