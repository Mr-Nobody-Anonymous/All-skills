name: file_integrity
version: 1.0.0
category: security
priority: 20
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - check file integrity
  - verify file hash
triggers:
  - "has this file changed"
  - "file hash"
  - "integrity check"
