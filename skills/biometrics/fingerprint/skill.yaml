name: fingerprint
version: 1.0.0
category: biometrics
priority: 15
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - fingerprint recognition
  - biometric auth
triggers:
  - "fingerprint"
  - "biometric"
  - "scan fingerprint"
