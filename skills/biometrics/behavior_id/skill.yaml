name: behavior_id
version: 1.0.0
category: biometrics
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - behavioral biometrics
  - behavior id
triggers:
  - "typing pattern"
  - "mouse pattern"
  - "behavioral auth"
