name: mqtt
version: 1.0.0
category: smart_home
priority: 15
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - publish mqtt
  - subscribe mqtt
triggers:
  - "mqtt publish"
  - "send mqtt"
  - "subscribe to topic"
