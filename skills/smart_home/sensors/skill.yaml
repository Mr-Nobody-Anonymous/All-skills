name: sensors
version: 1.0.0
category: smart_home
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - read sensor
  - sensor data
triggers:
  - "what is the temperature"
  - "humidity level"
  - "motion sensor"
