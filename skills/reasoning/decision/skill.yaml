name: decision
version: 1.0.0
category: reasoning
priority: 20
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - make decision
  - evaluate options
triggers:
  - "should I"
  - "compare options"
  - "best choice between"
