name: image_gen
version: 1.0.0
category: media
priority: 25
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - generate image
  - create image from text
triggers:
  - "generate an image of"
  - "draw"
  - "create picture"
