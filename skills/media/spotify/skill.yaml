name: spotify
version: 1.0.0
category: media
priority: 10
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - play spotify
  - spotify search
triggers:
  - "spotify"
  - "play on spotify"
  - "spotify playlist"
