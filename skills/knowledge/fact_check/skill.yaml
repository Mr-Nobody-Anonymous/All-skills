name: fact_check
version: 1.0.0
category: knowledge
priority: 35
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - verify fact
  - fact check claim
triggers:
  - "is it true that"
  - "fact check"
  - "verify"
