name: web_scrape
version: 1.0.0
category: knowledge
priority: 30
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - scrape website
  - extract web data
triggers:
  - "get data from"
  - "scrape this url"
  - "extract content from"
