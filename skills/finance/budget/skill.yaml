name: budget
version: 1.0.0
category: finance
priority: 20
dependencies:
  []
load_strategy: lazy
resources:
  gpu: false
  memory_mb: 256
platforms: [linux, macos, windows]
intents_handled:
  - track budget
  - add expense
triggers:
  - "log expense"
  - "track spending"
  - "budget"
