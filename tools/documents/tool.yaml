tool_id: documents
version: "2.0.0"
description: "Document parsing, conversion, and structured generation (PDF, DOCX, XLSX)"
capabilities: [document:parse, document:generate]
permissions:
  mode: "local-scratch"
risk_level: "low"
platforms: ["linux", "windows", "macos"]
healthcheck:
  command: "echo healthcheck"
  timeout_sec: 5
