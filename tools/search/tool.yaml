tool_id: search
version: "2.0.0"
description: "High-performance semantic and regex codebase searching"
capabilities: [search:grep, search:semantic]
permissions:
  mode: "read-only"
risk_level: "low"
platforms: ["linux", "windows", "macos"]
healthcheck:
  command: "echo healthcheck"
  timeout_sec: 5
