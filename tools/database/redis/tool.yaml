tool_id: database-redis
version: "2.0.0"
engine: "redis"
capabilities: ["database:query", "database:schema"]
permissions:
  default: "read-only"
  mutation_requires_confirmation: true
risk_level: "high"
