tool_id: database-postgres
version: "2.0.0"
engine: "postgres"
capabilities: ["database:query", "database:schema"]
permissions:
  default: "read-only"
  mutation_requires_confirmation: true
risk_level: "high"
