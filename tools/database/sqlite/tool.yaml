tool_id: database-sqlite
version: "2.0.0"
engine: "sqlite"
capabilities: ["database:query", "database:schema"]
permissions:
  default: "read-only"
  mutation_requires_confirmation: true
risk_level: "high"
