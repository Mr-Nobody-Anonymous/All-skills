tool_id: database-mysql
version: "2.0.0"
engine: "mysql"
capabilities: ["database:query", "database:schema"]
permissions:
  default: "read-only"
  mutation_requires_confirmation: true
risk_level: "high"
